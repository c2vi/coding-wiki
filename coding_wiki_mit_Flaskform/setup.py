from setuptools import setup
setup(
    name = 'coding-wiki-cli',
    version = '0.1.0',
    packages = [''],
    entry_points = {
        'console_scripts': [
            'cdw = main_cli:main'
         	
        ]
    })
